<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Menú Inferior</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css" rel="stylesheet">
    <style>
        
        .navbar-bottom {
            position: fixed;
            bottom: 0;
            left: 0;
            width: 100%;
            z-index: 1000;
            background-color: #00263A; 
            border-top: 1px solid rgba(255, 255, 255, 0.1);
        }
        .navbar-bottom .nav-link {
            color: rgba(255, 255, 255, 0.7);
            text-align: center;
            flex: 1;
            padding: 10px;
        }
        .navbar-bottom .nav-link.active {
            color: #fff;
            font-weight: bold;
        }
        .navbar-bottom .nav-link i {
            display: block;
            font-size: 1.5rem;
            margin-bottom: 5px;
        }
    </style>
</head>
<body>


    <!-- Menú Inferior -->
    <nav class="navbar navbar-bottom navbar-dark bg-dark">
        <div class="container-fluid">
            <a href="/" class="nav-link active" id="home-link">
                <i class="fas fa-home"></i>
                <span>Home</span>
            </a>
            <a href="/news" class="nav-link" id="noticias-link">
                <i class="fas fa-newspaper"></i>
                <span>Noticias</span>
            </a>
            <a href="/calendar" class="nav-link" id="calendario-link">
                <i class="fas fa-calendar-alt"></i>
                <span>Calendario</span>
            </a>
            <a href="/settings" class="nav-link" id="ajustes-link">
                <i class="fas fa-cog"></i>
                <span>Ajustes</span>
            </a>
            <a href="/profile" class="nav-link" id="perfil-link">
                <i class="fas fa-user"></i>
                <span>Perfil</span>
            </a>
        </div>
    </nav>

    <!-- Bootstrap JS y dependencias -->
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.11.6/dist/umd/popper.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.min.js"></script>
    <script>
        // Resaltar el enlace activo según la vista actual
        const currentPage = window.location.pathname;
        const links = document.querySelectorAll('.navbar-bottom .nav-link');

        links.forEach(link => {
            if (link.getAttribute('href') === currentPage) {
                link.classList.add('active');
            } else {
                link.classList.remove('active');
            }
        });
    </script>
</body>
</html>