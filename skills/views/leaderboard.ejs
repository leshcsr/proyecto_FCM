<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Leaderboard</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      text-align: center;
    }
    .badge-container {
      display: flex;
      flex-wrap: wrap;
      justify-content: center;
      gap: 20px;
      margin-top: 20px;
    }
    .badge {
      border: 1px solid #ccc;
      padding: 10px;
      border-radius: 8px;
      width: 150px;
      text-align: center;
      box-shadow: 2px 2px 8px rgba(0, 0, 0, 0.1);
    }
    .badge img {
      width: 100px;
      height: 100px;
      margin-bottom: 10px;
    }
    .badge h3 {
      font-size: 1rem;
      margin: 5px 0;
    }
    .badge p {
      font-size: 0.9rem;
      margin: 0;
      color: #555;
    }
  </style>
</head>
<body>
  <h1>Leaderboard</h1>
  <div class="badge-container">
    <!-- Medallas se insertarán aquí dinámicamente -->
    <% badges.forEach(badge => { %>
      <div class="badge">
        <img src="/badges/<%= badge.png %>" alt="<%= badge.rango %>">
        <h3><%= badge.rango %></h3>
        <p><%= badge.bitpoints_min %> - <%= badge.bitpoints_max %> puntos</p>
      </div>
    <% }); %>
  </div>
</body>
</html>
